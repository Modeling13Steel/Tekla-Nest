{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Tekla Nest \u2014 Documentation","text":"<p>Welcome. This site explains how to install, run, build, deploy and administer the Tekla Nest product family.</p>"},{"location":"#the-three-parts","title":"The three parts","text":"<p>Everything lives in one repository, split into three parts:</p> Part What it is Who touches it Lives in Nesting app The desktop program that optimises steel-bar cutting. End users; whoever builds the installer. <code>src/tekla_nest/</code> (single Python package) Admin console Desktop tool to issue, revoke and delete software licences. An administrator. <code>src/tekla_nest/admin/</code> (submodule of the same package) Infrastructure (license server) The small cloud service that checks licences. An operator, once, to deploy it. <code>license-server/</code> (Firebase Functions + Terraform) <p>There is no separate <code>tekla-common</code>/<code>tekla-admin-console</code> package \u2014 the nesting app and the admin console share code by living in the same <code>tekla_nest</code> package (config, i18n, branding, resources are shared modules under <code>src/tekla_nest/</code>).</p>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>Just want to get set up? \u2192 Getting started</li> <li>Using or building the desktop app? \u2192 Nesting app</li> <li>Managing licences? \u2192 Admin console</li> <li>Deploying the cloud license server? \u2192 Infrastructure</li> <li>A developer working on the code? \u2192 For developers</li> </ul> <p>Not very technical? Start here</p> <p>Read Getting started first \u2014 it lists exactly which accounts and tools you need, in plain language, before anything else.</p>"},{"location":"admin-console/","title":"Tekla Nest Admin Console","text":"<p>The Tekla Nest Admin Console is a Windows desktop admin app for managing Tekla Nest software licences.</p> <p>It is for administrators and support staff, not end users. Use it to:</p> <ol> <li>Issue a new customer licence.</li> <li>Revoke a licence.</li> <li>Release a machine binding so a customer can activate again.</li> <li>Delete a revoked or expired licence record.</li> <li>List licences and check their current status.</li> </ol> <p>The console talks to the Tekla Nest licence server over HTTPS \u2014 the <code>license-server/</code> Firebase Functions service in this repository (see Infrastructure).</p> Term Meaning Licence key The key given to a customer so Tekla Nest can be activated. Admin API key A secret token that lets an administrator call admin endpoints on the licence server. Bearer token The admin key is sent in the HTTPS <code>Authorization: Bearer &lt;admin-key&gt;</code> header. Machine binding The server record that says a licence has been activated on a specific computer."},{"location":"admin-console/#prerequisites-and-connection-settings","title":"Prerequisites and connection settings","text":"<p>You need two values before the console can connect.</p> Setting Where it comes from What the app does Licence server URL The deployed Firebase Functions base URL, for example <code>https://europe-west1-&lt;project-id&gt;.cloudfunctions.net</code> The GUI shows a License server URL field, pre-filled from the root <code>config.yaml</code>'s <code>licensing.server_url</code>; if blank or wrong, paste the URL manually. The app requires it to start with <code>http://</code> or <code>https://</code>. <code>ADMIN_API_KEY</code> The normal admin key stored as a Firebase Functions secret Paste it into the GUI Admin key field. The field is password-hidden. The GUI does not load it from an environment variable and does not save it for the next session. <p>When you click Connect and refresh or Test connection, the app creates an API client with those two values. The client removes any trailing slash from the server URL and sends the key as:</p> <pre><code>Authorization: Bearer &lt;admin-key&gt;\n</code></pre> <p>The Using recovery key checkbox only shows a warning in the GUI. It does not change the request. If you paste the <code>MASTER_ADMIN_API_KEY</code>, the same admin-key field is used.</p>"},{"location":"admin-console/#how-to-run-or-install-it","title":"How to run or install it","text":"<p>The admin console is part of the single <code>tekla-nest</code> package \u2014 not a separate workspace package. It lives in <code>src/tekla_nest/admin/</code> and shares config/i18n/branding with the nesting app.</p> Item Value Python package <code>tekla_nest</code> (submodule <code>tekla_nest.admin</code>) Entry point <code>tekla-nest-admin</code> Dev make target <code>make run-admin</code>"},{"location":"admin-console/#run-from-source","title":"Run from source","text":"<p>From the repository root:</p> <pre><code>make install\nmake run-admin\n</code></pre> <p><code>make run-admin</code> runs:</p> <pre><code>uv run tekla-nest-admin\n</code></pre>"},{"location":"admin-console/#install-on-windows","title":"Install on Windows","text":"<p>For normal admin users, install the Windows setup file:</p> <pre><code>tekla-nest-admin-setup.exe\n</code></pre> <p>This installer is built by the GitHub Actions workflow Build Windows Packages on Windows (<code>make installer-admin</code> locally, which needs Inno Setup \u2014 see Getting started). It is packaged separately from:</p> <ul> <li>the customer-facing Tekla Nest app installer;</li> <li>the licence-server infrastructure bundle.</li> </ul>"},{"location":"admin-console/#common-tasks","title":"Common tasks","text":""},{"location":"admin-console/#1-connect-to-the-licence-server","title":"1. Connect to the licence server","text":"<ol> <li>Launch Tekla Nest Admin.</li> <li>Enter the licence server base URL.</li> <li>Paste the normal <code>ADMIN_API_KEY</code>.</li> <li>Click Connect and refresh.</li> <li>Confirm the licence table loads.</li> </ol> <p>For a connection-only check, click Test connection.</p>"},{"location":"admin-console/#2-issue-a-licence","title":"2. Issue a licence","text":"<p>In the GUI:</p> <ol> <li>Click Create.</li> <li>Enter the customer or company name.</li> <li>Set Duration days.</li> <li>Default: <code>365</code> days.</li> <li>Allowed range in the GUI: <code>1</code> to <code>3650</code> days.</li> <li>Set Max machines.</li> <li>Default: <code>1</code> machine.</li> <li>Allowed range in the GUI: <code>1</code> to <code>25</code> machines.</li> <li>Click OK.</li> <li>The new licence key is copied to the clipboard.</li> <li>Send the licence key to the customer through the approved support channel.</li> </ol> <p>CLI equivalent, from <code>license-server/</code>:</p> <pre><code>python scripts/admin_cli.py create --customer \"Acme Corporation\" --days 365 --machines 1\n</code></pre>"},{"location":"admin-console/#3-revoke-a-licence","title":"3. Revoke a licence","text":"<p>Use this when a licence must no longer validate.</p> <p>In the GUI:</p> <ol> <li>Select the licence row.</li> <li>Click Revoke.</li> <li>Read the confirmation message.</li> <li>Confirm only if you are sure.</li> </ol> <p>CLI equivalent:</p> <pre><code>python scripts/admin_cli.py revoke --key &lt;license-key&gt;\n</code></pre> <p>Warning</p> <p>Revocation affects future online validation. If a customer is offline, the app may continue until its next required server validation.</p>"},{"location":"admin-console/#4-release-a-machine-binding","title":"4. Release a machine binding","text":"<p>Use this when a customer changes computer, reinstalls Windows, or needs to activate again.</p> <p>In the GUI, to release all machines for the selected licence:</p> <ol> <li>Select the licence row.</li> <li>Click Release all.</li> <li>Confirm the action.</li> <li>Ask the customer to activate again.</li> </ol> <p>To release one specific machine:</p> <ol> <li>Select the licence row.</li> <li>In License Detail, choose a machine under Machine to release.</li> <li>Click Release selected machine.</li> <li>Confirm the action.</li> </ol> <p>CLI equivalents:</p> <pre><code>python scripts/admin_cli.py release --key &lt;license-key&gt;\npython scripts/admin_cli.py release --key &lt;license-key&gt; --machine &lt;machine-id&gt;\n</code></pre>"},{"location":"admin-console/#5-delete-a-licence","title":"5. Delete a licence","text":"<p>Use this to permanently remove a licence record that's no longer needed \u2014 for example to clean up old revoked or expired licences from the list. This is a GUI-only action (there is currently no <code>admin_cli.py delete</code> command).</p> <ol> <li>Select a licence row whose status is Revoked or Expired.</li> <li>Click Delete.</li> <li>Read the confirmation message \u2014 this cannot be undone.</li> <li>Confirm only if you are sure.</li> </ol> <p>The Delete action is disabled for Active, Unactivated or Full licences \u2014 the server rejects the request unless the licence is already revoked or expired, so revoke it first if you need to remove an active licence.</p>"},{"location":"admin-console/#6-list-licences-and-check-status","title":"6. List licences and check status","text":"<p>In the GUI:</p> <ol> <li>Click Refresh to reload all licences.</li> <li>Use the search box to find a customer, licence key, or machine.</li> <li>Use the status filter for Active, Unactivated, Full, Expired, or Revoked.</li> <li>Select a row to see details such as expiry date, days remaining, machines, activation time, and last validation time.</li> <li>Click Status to fetch the latest server status for the selected licence.</li> <li>Use Copy license key if you need to paste the selected key into a support message.</li> </ol> <p>CLI equivalents:</p> <pre><code>python scripts/admin_cli.py list\npython scripts/admin_cli.py status --key &lt;license-key&gt;\n</code></pre>"},{"location":"admin-console/#command-line-alternative","title":"Command-line alternative","text":"<p>The script <code>license-server/scripts/admin_cli.py</code> is supported for scripting, automation, and fallback support work. It reuses the same <code>tekla_nest.admin.api_client.AdminApiClient</code> as the GUI (create/list/status/revoke/release only \u2014 delete is GUI-only, see above).</p> <p>Set these environment variables first:</p> <pre><code>export LICENSE_SERVER_URL=\"https://europe-west1-&lt;project-id&gt;.cloudfunctions.net\"\nexport ADMIN_API_KEY=\"$ADMIN_KEY\"\n</code></pre> <p>Then run commands from <code>license-server/</code>:</p> <pre><code>python scripts/admin_cli.py list\n</code></pre> <p>On Windows, if <code>python</code> isn't recognised, use the launcher instead: <code>py scripts\\admin_cli.py list</code> (see Getting started).</p>"},{"location":"admin-console/#security-notes","title":"Security notes","text":"<p>Protect admin keys</p> <p><code>ADMIN_API_KEY</code> and <code>MASTER_ADMIN_API_KEY</code> can perform admin licence operations. Never commit them, paste them into issue trackers, include them in screenshots, or store them in release artifacts.</p> Key Use Storage <code>ADMIN_API_KEY</code> Normal daily admin work Team password vault. Rotate if exposed or when staff access changes. <code>MASTER_ADMIN_API_KEY</code> Break-glass recovery only, for example when the normal key is lost Separate restricted vault or offline recovery store. <p>Break-glass process:</p> <ol> <li>Use <code>MASTER_ADMIN_API_KEY</code> only when the normal <code>ADMIN_API_KEY</code> is unavailable.</li> <li>Paste it into the same Admin key field.</li> <li>Check Using recovery key so the warning is visible.</li> <li>Do only the recovery task.</li> <li>Rotate the normal <code>ADMIN_API_KEY</code> immediately if it was lost.</li> <li>Close the admin app and return to the normal key.</li> </ol> <p>The master key does not unlock the customer desktop app. It only authorizes the same licence-server admin endpoints as the normal admin key.</p>"},{"location":"admin-console/#small-commands-table","title":"Small commands table","text":"Task Command Install dependencies <code>make install</code> Run admin GUI from source <code>make run-admin</code> Run the entry point directly <code>uv run tekla-nest-admin</code> Set CLI server URL <code>export LICENSE_SERVER_URL=\"https://europe-west1-&lt;project-id&gt;.cloudfunctions.net\"</code> Set CLI admin key <code>export ADMIN_API_KEY=\"$ADMIN_KEY\"</code> CLI: create licence <code>python scripts/admin_cli.py create --customer \"Acme\" --days 365 --machines 1</code> CLI: list licences <code>python scripts/admin_cli.py list</code> CLI: check status <code>python scripts/admin_cli.py status --key &lt;license-key&gt;</code> CLI: revoke licence <code>python scripts/admin_cli.py revoke --key &lt;license-key&gt;</code> CLI: release all machines <code>python scripts/admin_cli.py release --key &lt;license-key&gt;</code> CLI: release one machine <code>python scripts/admin_cli.py release --key &lt;license-key&gt; --machine &lt;machine-id&gt;</code> Delete licence GUI only \u2014 select a Revoked/Expired row \u2192 Delete"},{"location":"development/","title":"For developers","text":"<p>Technical reference for people working on the source code.</p>"},{"location":"development/#repository-layout","title":"Repository layout","text":"<pre><code>src/tekla_nest/                single Python package (project name: tekla-nest)\n\u251c\u2500\u2500 admin/                     admin console (submodule, not a separate package)\n\u251c\u2500\u2500 config/                    YAML configuration loader\n\u251c\u2500\u2500 models/                    dataclasses (CutPiece, StockBar, BarResult, NestResult)\n\u251c\u2500\u2500 presenters/                MVP presenter (providers/services \u2194 views)\n\u251c\u2500\u2500 providers/                 part sources (Tekla, CSV, manual entry)\n\u251c\u2500\u2500 services/                  business logic (nest engine, CSV, Tekla API bridge, PDF report)\n\u2514\u2500\u2500 views/                     PySide6 GUI widgets\nlicense-server/                Firebase Functions + Terraform (license server)\n\u251c\u2500\u2500 functions/                 Python Cloud Functions (main.py, requirements.txt)\n\u251c\u2500\u2500 scripts/                   admin_cli.py, deploy.py, generate_keys.py, ...\n\u2514\u2500\u2500 terraform/                 optional IaC for one-time cloud project setup\nresources/                     logos, translations (languages/*.yaml), report template\ntests/                         test suite (nesting app + admin console)\ndocs/ + mkdocs.yml              this documentation site\nMakefile                        task runner\npyproject.toml                   single package config + ruff/pytest config\n</code></pre> <p>There is no <code>tekla_package/</code> monorepo and no <code>tekla-common</code>/<code>tekla-admin-console</code>/<code>tekla-iac</code> sub-packages \u2014 that is an older/aspirational layout that a previous docs pass described but was never actually built. The nesting app and the admin console share code by both living inside the one <code>tekla_nest</code> package; the license server is its own top-level <code>license-server/</code> folder (not a uv workspace member \u2014 it has its own Python dependencies via <code>functions/requirements.txt</code> and is driven by the Firebase CLI, not <code>uv</code>).</p>"},{"location":"development/#commands","title":"Commands","text":"Command What it does <code>make install</code> Install runtime dependencies (<code>uv sync</code>). <code>make install-dev</code> Install project + dev extras (pytest, pytest-qt, ruff). <code>make install-all</code> Install dev extras + the Windows-only <code>tekla</code> extra (pythonnet). <code>make run</code> / <code>make run-admin</code> Launch the Nesting app / Admin console. <code>make demo</code> Run the CLI demo with sample data (<code>CSV=parts.csv</code> to use your own file). <code>make lint</code> / <code>make format</code> Ruff lint / auto-format. <code>make test</code> / <code>make test-fast</code> / <code>make test-cov</code> Run the test suite (offscreen Qt), a fast subset, or with coverage. <code>make check</code> Lint + test. <code>make build</code> Build wheel + sdist. <code>make binary</code> / <code>make binary-admin</code> Standalone PyInstaller executables. <code>make installer</code> / <code>make installer-admin</code> Windows installers (needs Inno Setup, <code>iscc</code> on PATH). <code>make installers</code> Both installers in one step. <code>make python-embed</code> Windows only: prepare the bundled Python + pythonnet used inside the installer (see Getting started). <code>make infrastructure-package</code> Zip <code>license-server/</code> (minus secrets/venv) for distribution. <code>make deploy-infrastructure</code> Deploy the license-server Firebase infrastructure \u2014 see Infrastructure. Runs <code>python3.12 scripts/deploy.py</code>; on Windows run that script directly with <code>py -3.12 scripts\\deploy.py</code>. <code>make clean</code> / <code>make clean-all</code> Remove caches/build artifacts. <code>make help</code> List everything. <p>There is no <code>make bootstrap</code> target. Use <code>make install</code> (or <code>make install-dev</code> / <code>make install-all</code>) instead.</p>"},{"location":"development/#tests","title":"Tests","text":"<pre><code>make test          # QT_QPA_PLATFORM=offscreen uv run pytest\n</code></pre> <ul> <li>Runs headless (offscreen Qt). <code>tests/conftest.py</code> stubs modal dialogs so the suite doesn't   block waiting for a click.</li> <li>Windows/Tekla-specific tests are marked <code>windows_only</code> and skipped off Windows:   <code>uv run pytest -m \"not windows_only\"</code>.</li> </ul>"},{"location":"development/#code-quality","title":"Code quality","text":"<pre><code>uv run ruff check .\nuv run ruff format .\n</code></pre> <p>There is currently no <code>.pre-commit-config.yaml</code> in this repo \u2014 linting/formatting is run manually (or via CI) with the commands above, not through pre-commit hooks.</p>"},{"location":"development/#continuous-integration-github-actions","title":"Continuous integration (GitHub Actions)","text":"Workflow Runs on Does <code>build-windows.yml</code> Windows Builds both <code>.exe</code> installers + the infra zip; attaches them to a <code>v*</code> release, or runs on manual dispatch/push to <code>main</code>. <code>docs.yml</code> Ubuntu Builds this MkDocs site (<code>mkdocs build --strict</code>) and deploys it to GitHub Pages on push to <code>main</code>. <p>There is currently no lint/test CI workflow (<code>ci.yml</code>) and no Terraform-validation workflow (<code>iac.yml</code>) in <code>.github/workflows/</code> \u2014 run <code>make check</code> locally before pushing. <code>build-windows.yml</code> builds on <code>windows-latest</code> on every push to <code>main</code> and on tags, so it is exercised regularly (not just before a release).</p>"},{"location":"development/#shared-runtime-data","title":"Shared runtime data","text":"<p><code>config.yaml</code> and the <code>resources/</code> tree (logos, translations, report template) live at the project root and are bundled with the package. At runtime the base directory resolves next to the installed package (or <code>sys._MEIPASS</code> in a frozen PyInstaller build). Re-brand by editing the root <code>config.yaml</code> and <code>resources/</code>, not code.</p>"},{"location":"getting-started/","title":"Getting started","text":"<p>This page is the starting point. It lists the accounts and tools you need, then points you to the right place for each task. No prior experience assumed.</p>"},{"location":"getting-started/#1-what-do-you-want-to-do","title":"1. What do you want to do?","text":"Goal Go to Use the desktop app to plan steel cuts Nesting app Build the app installer (<code>.exe</code>) Nesting app \u2192 building Issue, revoke, release or delete customer licences Admin console Set up the cloud license server (one time) Infrastructure Work on the source code For developers"},{"location":"getting-started/#2-accounts-you-may-need","title":"2. Accounts you may need","text":"Account Needed for Cost GitHub account Getting the code; running the automated builds. Free Google / Firebase account Only if you deploy the cloud license server. Free tier (a payment card is required, but usage is ~\u20ac0 at this scale). <p>Note</p> <p>End users of the desktop app need no accounts \u2014 they just run the installer.</p>"},{"location":"getting-started/#3-tools-to-install-for-maintainersdevelopers","title":"3. Tools to install (for maintainers/developers)","text":"<p>You only need these if you will run the code from source or build/deploy it \u2014 not for end users. The table below is the short version; see \u00a74 for Windows-specific gotchas (PATH, the Python launcher, venv activation).</p> Tool Why Where Python 3.12 Runs the apps and the tooling. The project supports <code>&gt;=3.9</code>, but 3.12 is what CI, the Windows build and the license-server scripts use \u2014 install 3.12 unless you have a specific reason not to. https://www.python.org/downloads/ uv Installs dependencies and manages the virtual environment in one step. https://docs.astral.sh/uv/ Git Get and manage the code. https://git-scm.com/ <p>Only needed if you will deploy or work on the license server (<code>license-server/</code>):</p> Tool Why Where Node.js (includes npm) The Firebase CLI is distributed as an npm package. https://nodejs.org (LTS) Firebase CLI Deploys the Cloud Functions, manages secrets. Installed with <code>npm install -g firebase-tools</code> once Node/npm is present. https://firebase.google.com/docs/cli Google Cloud CLI (<code>gcloud</code>) One-time project setup (enable APIs, create the Firestore database, IAM fixes) and auth. https://cloud.google.com/sdk/docs/install Terraform (optional) Alternative, scripted way to do the one-time cloud setup instead of manual <code>gcloud</code>/Console steps. https://developer.hashicorp.com/terraform/install <p>Only needed if you will build the Windows installer (see Nesting app \u2192 building):</p> Tool Why Where Inno Setup (<code>iscc</code> on PATH) Packages the PyInstaller build into <code>tekla-nest-setup.exe</code>. https://jrsoftware.org/isinfo.php Visual C++ Redistributable Bundled with the installer for the target machine; not needed on the build machine. Downloaded automatically by CI. <p>The easiest path for the Windows installer is to let GitHub Actions build it for you (push a version tag, or trigger the Build Windows Packages workflow manually) \u2014 see Nesting app. Building it locally is only needed if you can't use CI.</p>"},{"location":"getting-started/#4-windows-notes-path-shims-venvs","title":"4. Windows notes: PATH, shims, venvs","text":"<p>These are the gotchas that trip people up on Windows specifically.</p>"},{"location":"getting-started/#the-python-shim-two-different-things-share-that-name-here","title":"The Python \"shim\" \u2014 two different things share that name here","text":"<ol> <li> <p>The <code>py</code> launcher, installed alongside Python from python.org. Windows does not    create a <code>python3.12.exe</code>/<code>python3.exe</code> the way macOS/Linux package managers do \u2014 only    <code>python.exe</code> plus the launcher <code>py.exe</code>. Anywhere these docs (or <code>license-server/GUIDE.md</code>)    say <code>python3.12 &lt;script&gt;</code>, on Windows use:</p> <pre><code>py -3.12 &lt;script&gt;\n</code></pre> <p>Check the launcher can see 3.12 with <code>py -0p</code> (lists installed versions and paths).</p> </li> <li> <p>The bundled/portable Python built by <code>make python-embed</code>    (<code>scripts/prepare-python-embed.ps1</code>, Windows only). This downloads the official Python    embeddable package, enables <code>site-packages</code>, and installs <code>pythonnet</code> into it. It is    not for running project scripts \u2014 it is copied into the installer so the frozen    <code>tekla-nest.exe</code> has a real Python + <code>pythonnet</code>/CLR bridge available at runtime for the    Tekla Structures Open API integration (PyInstaller-frozen apps can't load <code>pythonnet</code>    directly). You do not need to run this yourself unless building the installer locally \u2014    CI does it automatically.</p> </li> </ol>"},{"location":"getting-started/#path","title":"PATH","text":"<p>After installing each tool above, confirm it's on <code>PATH</code> in a new terminal (PATH changes don't apply to already-open shells):</p> <pre><code>py -0p                 # Python launcher sees your installs\nuv --version\ngit --version\nnode --version &amp;&amp; npm --version\nfirebase --version\ngcloud --version\niscc /?                # Inno Setup (only if building the installer locally)\n</code></pre> <p>If a tool was \"installed\" via <code>winget</code>/<code>choco</code> but still isn't found, open a new terminal \u2014 these installers update the system <code>PATH</code>, which existing shells don't pick up.</p>"},{"location":"getting-started/#virtual-environments","title":"Virtual environments","text":"<p><code>uv sync</code> / <code>uv run</code> create and use a <code>.venv/</code> automatically \u2014 you generally don't need to activate it manually. If you do (e.g. to run a plain <code>python</code> command inside it):</p> <pre><code># Windows (PowerShell)\n.venv\\Scripts\\Activate.ps1\n</code></pre> <pre><code># macOS / Linux\nsource .venv/bin/activate\n</code></pre> <p>If PowerShell refuses to run the activation script (\"running scripts is disabled on this system\"), that's the execution policy \u2014 either use <code>uv run &lt;command&gt;</code> instead of activating at all, or run PowerShell scripts for this repo with:</p> <pre><code>powershell -ExecutionPolicy Bypass -File &lt;script&gt;.ps1\n</code></pre> <p>(this is exactly how <code>make python-embed</code> invokes <code>prepare-python-embed.ps1</code>).</p>"},{"location":"getting-started/#5-one-time-setup-developersmaintainers","title":"5. One-time setup (developers/maintainers)","text":"<p>From the repository root:</p> <pre><code>make install       # or: uv sync                \u2014 runtime dependencies\nmake install-dev    # + dev tools (pytest, pytest-qt, ruff)\nmake run            # launches the Nesting app\nmake run-admin       # launches the Admin console\nmake help            # shows every available command\n</code></pre> <p>On Windows without <code>make</code> (e.g. plain Command Prompt), run the underlying <code>uv</code>/<code>uv run</code> commands shown after each target directly, or use Git Bash/WSL where <code>make</code> works normally.</p> <p>That's it \u2014 you now have a working environment. Continue on the page for your task. Setting up the license server or building the Windows installer need the extra tools listed in \u00a73 \u2014 see Infrastructure and Nesting app respectively.</p>"},{"location":"infrastructure/","title":"Infrastructure (license server)","text":"<p>This is the small cloud service that checks whether a Tekla Nest licence is valid. You deploy it once. After that it mostly runs itself.</p> <p>You only need this if you run your own license server</p> <p>If someone already deployed it and gave you a server address, you can skip to After deploying. This page is for the person doing the one-time setup.</p>"},{"location":"infrastructure/#1-what-it-is-plain-words","title":"1. What it is (plain words)","text":"<pre><code>Desktop apps  \u2500\u2500HTTPS\u2500\u2500\u25ba  License server (Firebase Cloud Functions)  \u2500\u2500\u25ba  Firestore database\n(Nesting / Admin)          /activate  /validate  /admin_*                 (stores licences)\n</code></pre> <ul> <li>Firebase / Google Cloud \u2014 Google's cloud platform. \"Firebase\" is the friendly layer on   top of Google Cloud.</li> <li>Cloud Functions \u2014 small programs that run on demand in the cloud (our 7 endpoints).</li> <li>Firestore \u2014 the cloud database that stores the licence records.</li> <li>Secret Manager \u2014 a safe place for the secret keys.</li> <li>Terraform \u2014 an optional tool that can create the cloud pieces from a script.</li> </ul> <p>At the small volumes this service handles, the cost is effectively \u20ac0 (free tier); a payment card is still required on the account.</p>"},{"location":"infrastructure/#2-accounts-to-create","title":"2. Accounts to create","text":"<ol> <li>A Google account (if you don't have one).</li> <li>A Firebase project at https://console.firebase.google.com \u2192 Add project.</li> <li>Enable billing on that project (Google Cloud \u2192 Billing). Cloud Functions require the    pay-as-you-go Blaze plan, which needs a card \u2014 but usage stays within the free tier.</li> </ol> <p>Warning</p> <p>Keep the project ID handy (e.g. <code>tekla-nest-licences</code>). You'll use it in the commands below wherever it says <code>&lt;project-id&gt;</code>.</p>"},{"location":"infrastructure/#3-tools-to-install","title":"3. Tools to install","text":"Tool Install Node.js + Firebase CLI Node from https://nodejs.org, then <code>npm install -g firebase-tools</code> Google Cloud CLI (<code>gcloud</code>) https://cloud.google.com/sdk/docs/install Python 3.12 https://www.python.org/downloads/ Terraform (optional) https://developer.hashicorp.com/terraform/install <p>All commands below run from the <code>license-server/</code> folder.</p>"},{"location":"infrastructure/#4-deploy-roadmap-do-these-in-order","title":"4. Deploy roadmap (do these in order)","text":"<p>Note</p> <p>This mirrors the authoritative runbook, <code>license-server/GUIDE.md</code>. If anything here differs, the GUIDE is the source of truth.</p> <p>Step 1 \u2014 Sign in <pre><code>firebase login\ngcloud auth login\nexport PROJECT_ID=\"&lt;project-id&gt;\"\n</code></pre></p> <p>Step 2 \u2014 Turn on the database <pre><code>gcloud services enable firestore.googleapis.com --project=\"$PROJECT_ID\"\ngcloud firestore databases create --location=eur3 --project=\"$PROJECT_ID\"\n</code></pre></p> <p>Step 3 \u2014 Generate the signing keys (creates <code>keys/private_key.pem</code> + <code>keys/public_key.pem</code>) <pre><code>python scripts/generate_keys.py\n</code></pre></p> <p>Never share or commit the private key</p> <p><code>keys/private_key.pem</code> lets the server issue licences \u2014 treat it like a master password. Only the public key is embedded in the desktop app.</p> <p>Step 4 \u2014 Prepare the Functions environment (the Firebase CLI expects a local venv)</p> <p>This script requires exactly Python 3.12.</p> <pre><code>python3.12 scripts/bootstrap_functions_venv.py\n</code></pre> <p>On Windows, <code>python3.12</code> is usually not on PATH \u2014 use the launcher instead: <pre><code>py -3.12 scripts\\bootstrap_functions_venv.py\n</code></pre> (see Getting started).</p> <p>Step 5 \u2014 Store the three secrets <pre><code>firebase functions:secrets:set JWT_PRIVATE_KEY &lt; keys/private_key.pem\nprintf \"%s\" \"&lt;your-admin-key&gt;\"  | firebase functions:secrets:set ADMIN_API_KEY\nprintf \"%s\" \"&lt;your-master-key&gt;\" | firebase functions:secrets:set MASTER_ADMIN_API_KEY\n</code></pre></p> <p>Step 6 \u2014 Deploy the server <pre><code>firebase deploy --only functions\n</code></pre></p> <p>When it finishes, Firebase prints the function URLs \u2014 note the base address, e.g. <code>https://europe-west1-&lt;project-id&gt;.cloudfunctions.net</code>.</p> <p>Optional: Terraform instead of manual setup</p> <p>Steps 2 and part of 5 can be provisioned with Terraform (Firestore, Secret Manager, IAM): <pre><code>cd terraform\ncp terraform.tfvars.example terraform.tfvars   # fill in project_id, region, firestore_location\nterraform init &amp;&amp; terraform apply\n</code></pre> The Functions themselves are still deployed with <code>firebase deploy</code> (Step 6).</p>"},{"location":"infrastructure/#5-environment-configuration","title":"5. Environment / configuration","text":"Where Setting <code>terraform/terraform.tfvars</code> <code>project_id</code>, <code>region</code>, <code>firestore_location</code> <code>.firebaserc</code> the Firebase project alias <code>functions/main.py</code> function region (default <code>europe-west1</code>)"},{"location":"infrastructure/#6-common-deploy-problems","title":"6. Common deploy problems","text":"Symptom Fix (from GUIDE.md) Deploy fails on a brand-new project (build service account permission) Grant the Cloud Build role: <code>gcloud projects add-iam-policy-binding \"$PROJECT_ID\" --member=... --role=\"roles/cloudbuild.builds.builder\"</code>, then redeploy. <code>/activate</code> returns 403 after deploy Allow public invocation: <code>gcloud run services add-iam-policy-binding activate --member=\"allUsers\" --role=\"roles/run.invoker\" ...</code>. Deploy fails: missing <code>functions/venv</code> Re-run Step 4 (<code>bootstrap_functions_venv.py</code>). <p>See <code>license-server/GUIDE.md</code> for the exact troubleshooting commands.</p>"},{"location":"infrastructure/#7-after-deploying","title":"7. After deploying","text":"<p>Point the desktop apps at your server: set <code>licensing.server_url</code> in the repository root's <code>config.yaml</code> to the base URL from Step 6. Then use the Admin console to issue licences.</p>"},{"location":"infrastructure/#8-secrets-security","title":"8. Secrets &amp; security","text":"Secret Purpose Storage <code>JWT_PRIVATE_KEY</code> Signs licence tokens Firebase secret; generated locally; never commit <code>ADMIN_API_KEY</code> Normal admin operations Team password vault <code>MASTER_ADMIN_API_KEY</code> Emergency break-glass Separate restricted vault <ul> <li><code>keys/*.pem</code> are git-ignored \u2014 never commit them.</li> <li>Rotate keys per the runbook if exposed.</li> </ul>"},{"location":"infrastructure/#9-about-the-automated-checks","title":"9. About the automated checks","text":"<p>There is currently no automated CI validation for the Terraform/Functions code (no <code>iac.yml</code> workflow exists yet) \u2014 <code>terraform fmt</code>/<code>validate</code> and any Functions syntax checks must be run manually before deploying. Deployment itself is always the manual, operator-run process above (or <code>license-server/scripts/deploy.py</code>, see <code>license-server/GUIDE.md</code>) \u2014 CI never deploys infrastructure automatically.</p>"},{"location":"nesting-app/","title":"Nesting app","text":"<p>The Nesting app is the desktop program your team uses day-to-day. It takes a list of steel parts and works out how to cut them from stock bars with the least waste, then produces a printable cutting plan.</p>"},{"location":"nesting-app/#what-it-does","title":"What it does","text":"<ul> <li>Reads the parts to cut from Tekla Structures (directly, on Windows) or from a CSV file.</li> <li>Calculates an optimal 1D cutting plan (which pieces come from which bar).</li> <li>Exports the plan as HTML, PDF or Excel.</li> </ul>"},{"location":"nesting-app/#for-end-users","title":"For end users","text":""},{"location":"nesting-app/#install","title":"Install","text":"<ol> <li>Get the installer file <code>tekla-nest-setup.exe</code> (from your maintainer, or the GitHub    Releases page).</li> <li>Double-click it and follow the prompts.</li> <li>Launch Tekla Nest from the Start menu.</li> </ol>"},{"location":"nesting-app/#use-it-in-a-nutshell","title":"Use it (in a nutshell)","text":"<ol> <li>Load parts \u2014 click Import from Tekla (Windows + Tekla running) or Load CSV.</li> <li>Add or auto-generate the stock bars you cut from.</li> <li>Click Calculate.</li> <li>Export the plan as PDF, Excel or CSV.</li> </ol> <p>Licence</p> <p>On first use the app may ask for a licence key (issued by an administrator \u2014 see the Admin console). Once activated it keeps working offline.</p>"},{"location":"nesting-app/#for-maintainers-running-from-source","title":"For maintainers (running from source)","text":"<p>Prerequisites: Python 3.12, uv, Git (see Getting started).</p> <pre><code># from the repository root\nmake install        # one-time: install runtime dependencies\nmake run            # launch the Nesting app  (= uv run tekla-nest)\n</code></pre> Command Does <code>make install</code> Install runtime dependencies. <code>make install-dev</code> Install dev tools too (pytest, ruff). <code>make run</code> Launch the Nesting app. <code>make test</code> Run the tests. <code>make lint</code> Check code style."},{"location":"nesting-app/#building-the-windows-installer","title":"Building the Windows installer","text":"<p>The installer is built on Windows (it needs PyInstaller + Inno Setup + a bundled Python with <code>pythonnet</code>). The easy, supported way is GitHub Actions \u2014 you don't need a Windows PC yourself:</p> <ol> <li>Push a version tag, e.g. <code>git tag v2.1.0 &amp;&amp; git push --tags</code> \u2014 or open the repo on    GitHub \u2192 Actions \u2192 Build Windows Packages \u2192 Run workflow.</li> <li>When it finishes, download <code>tekla-nest-setup.exe</code> from the workflow's Artifacts    (a tagged run also attaches it to the GitHub Release).</li> </ol> <p>Note</p> <p>The installer cannot be built on macOS or Linux. Use the GitHub Action (or a Windows machine with Inno Setup + <code>make python-embed</code>) \u2014 see For developers and Getting started for the Windows-specific tooling (Inno Setup, the bundled Python \"shim\").</p>"},{"location":"nesting-app/#troubleshooting","title":"Troubleshooting","text":"Symptom Cause / fix Import from Tekla is greyed out Tekla integration needs Windows + Tekla Structures (2021\u20132026) running. On other systems, use Load CSV. App won't start after building from source Run <code>make install</code> again; confirm Python 3.12 (<code>python --version</code>, or <code>py -0p</code> on Windows). Asks for a licence Get a key from an administrator (Admin console)."},{"location":"releases/v2.1.0/","title":"Tekla Nest v2.1.0 \u2014 UI Refactor Release","text":"<p>Date: 2026-05 Tag: <code>v2.1.0</code> Type: Minor (UX overhaul, no breaking API changes)</p>"},{"location":"releases/v2.1.0/#highlights","title":"Highlights","text":"<p>A 9-milestone UI refactor lands the new shell:</p> <ul> <li>Design system \u2014 typography, colour, spacing, motion, and dark   theme tokens authored as a single source of truth   (<code>src/tekla_nest/design_system/</code>).</li> <li>AA contrast in both themes \u2014 verified by automated tests   (<code>test_dark_contrast.py</code>, body text \u2265 4.5:1, UI \u2265 3:1).</li> <li>Dark mode + Preferences menu \u2014 <code>Preferences \u2192 Theme</code> with   System / Light / Dark; default is Light. Persisted to   <code>~/.tekla_nest/preferences.json</code>.</li> <li>Motion \u2014 palette fade (120 ms), sidebar reveal (200 ms),   KPI count-up (180 ms). All gated by <code>Preferences \u2192 Reduced   motion</code>. Hard 200 ms cap enforced as <code>ValueError</code> in the helper.</li> <li>Tables polish \u2014 material chip filter, sticky filter bar,   shared <code>TableFilterProxyModel</code>.</li> <li>Insight sidebar + command palette + status bar polish.</li> <li>Shell slimmed \u2014 <code>nest_window.py</code> from ~450 LOC at v2.0 to   314 LOC at v2.1, with menu/i18n/preference logic extracted   to dedicated modules.</li> <li>Lint tightened \u2014 ruff <code>select</code> now <code>E, F, W, I, B, UP, SIM</code>;   Python target aligned to <code>py312</code>.</li> </ul>"},{"location":"releases/v2.1.0/#milestone-index","title":"Milestone index","text":"# Title Doc M1 App chrome M1-app-chrome.md M2 KPI strip + status bar M2-kpi-status.md M3 Insight sidebar M3-insight-sidebar.md M4 Command palette M4-command-palette.md M5 Material chips M5-material-chips.md M6 Tables polish M6-tables.md M7 Dark mode M7-dark-mode.md M8 Motion + reduced-motion M8-motion.md M9 Cleanup + release M9-release.md"},{"location":"releases/v2.1.0/#test-surface","title":"Test surface","text":"<ul> <li>394 tests passing (up from 273 at v2.0.0).</li> <li>Ruff clean under tightened ruleset.</li> <li>LOC budgets enforced via <code>scripts/check_loc.py</code>.</li> </ul>"},{"location":"releases/v2.1.0/#upgrade-notes","title":"Upgrade notes","text":"<ul> <li>No public API changes. Existing presenters, services, models   unchanged.</li> <li><code>AppConfig.prefer_reduced_motion: bool = False</code> is a new optional   field; older <code>config.yaml</code> files load unchanged.</li> <li><code>~/.tekla_nest/preferences.json</code> is created on first toggle of   theme or reduced motion. Safe to delete \u2014 it'll regenerate.</li> </ul>"},{"location":"releases/v2.1.0/#known-caveats","title":"Known caveats","text":"<ul> <li>The Preferences toggle for reduced motion takes effect immediately   but the persisted value isn't yet re-read into <code>AppConfig</code> on next   start (the file holds the choice; the next pass will layer it on).</li> <li>Baseline screenshots have not been re-captured for v2.1 (deferred:   requires a manual display session).</li> </ul>"},{"location":"releases/v2.1.0/#credits","title":"Credits","text":"<p>Built collaboratively with GitHub Copilot CLI.</p>"}]}